import type { Metadata, Viewport } from 'next'
import { Inter, JetBrains_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
})

const jetbrainsMono = JetBrains_Mono({ 
  subsets: ["latin"],
  variable: '--font-jetbrains',
})

export const metadata: Metadata = {
  title: 'LamadiAI | Enterprise AI Operations Assistant',
  description: 'Enterprise-grade AI operations assistant for restaurants, retail, and salons. Automate calls, manage appointments, and streamline customer interactions with intelligent voice and chat.',
  generator: 'v0.app',
  keywords: ['AI assistant', 'operations', 'restaurant AI', 'retail automation', 'salon booking', 'voice AI', 'phone automation', 'customer service AI'],
  openGraph: {
    title: 'LamadiAI | Enterprise AI Operations Assistant',
    description: 'Enterprise-grade AI operations assistant for restaurants, retail, and salons.',
    type: 'website',
  },
  icons: {
    icon: [
      { url: '/icon-light-32x32.png', media: '(prefers-color-scheme: light)' },
      { url: '/icon-dark-32x32.png', media: '(prefers-color-scheme: dark)' },
      { url: '/icon.svg', type: 'image/svg+xml' },
    ],
    apple: '/apple-icon.png',
  },
}

export const viewport: Viewport = {
  themeColor: '#0a0a12',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="dark bg-background">
      <body className={`${inter.variable} ${jetbrainsMono.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
